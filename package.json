{
  "name": "report-translator",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "LinguaDoc — free Cloudflare Worker that translates .txt/.docx into Word & PowerPoint reports",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
